# 测试与交付验证报告

验证日期：2026-07-22  
运行环境：Windows、Python 3.11.9、SQLite

## 自动测试

- 命令：`python -m pytest --cov=app --cov-report=term-missing`
- 结果：53 passed
- 应用代码总覆盖率：73%；技术分析核心模块覆盖率：93%
- 测试不依赖实时行情、真实 X Cookie 或真实 LLM 费用。

覆盖范围包括：账户与持仓 CRUD、现金参与仓位分母、盈亏、纪律规则、CSV 去重、行情多源降级、X 与 AI 安全降级、策略回测、技术信号、公司研究，以及交易计划生成器的四类状态、12 道闸门、风险/现金/单股/总仓位/行业集中度、100 股取整、止损距离、确认加仓、硬止损、第一减仓、版本冻结、AI Schema、跨股票/不存在来源/虚构数字/规则覆盖拦截、未配置降级和证据缓存失效。当前共 27 张业务表。

## 静态与迁移验证

- `python -m ruff check app tests`：通过。
- SQLite 已执行到 `20260722_0006 (head)`：通过；迁移保留原交易计划和持仓数据。
- `python -m alembic check`：无待生成的迁移。
- `docker-compose.yml`：YAML 结构检查通过，包含 app 与 MySQL 健康检查。
- Docker 实跑：当前电脑未安装 Docker CLI，未能执行。

## 实际启动检查

通过真实 Uvicorn 进程访问：

- `/api/v1/health`：200，数据库状态 `ok`；
- `/dashboard`、`/market`、`/technical`、`/x-monitor`、`/backtests`：200；
- 静态资源和 OpenAPI：200。
- `GET /api/v1/technical/000001`：180 根 K 线完成分析；
- `POST /api/v1/technical/000001/backtest`：vectorbt 五类信号回测成功。
- `/backtests`：浏览器实测中文参数标签、动态规则说明、对比表格、样本不足标记、资金/回撤画布、逐笔交易、普通中文结论和默认折叠 JSON 均正常；浏览器控制台无脚本错误。
- `/company-research`：浏览器实测真实 `300502`，财务结论/趋势图、红黄灰公告卡、公告链接、PE/PB/PS 分位、同行比较、三情景、证据与折叠 JSON 均正常。
- `/trade-plans`：浏览器实测新版生成器表单、规则版本、`INSUFFICIENT_DATA` 中文结果、12 道闸门、仓位计算、止损/确认加仓/退出、AI 未配置安全降级和默认折叠技术详情；控制台无脚本错误。
- 真实 `300502` 数据只读验证：243 根本地前复权日线，来源 `akshare_tencent_qfq`，分析日期 2026-07-22；识别到 2026-06-25 放量突破及后续回踩，未识别到再次转强，规则结果为 `NO_TRADE`（未保存测试计划）。

## 外部服务检查

- AKShare 1.18.72 已安装。东方财富接口在本机代理环境返回 `ProxyError`，自动降级后已用 `300502` 实测：新浪实时价成功；腾讯前复权日线返回 242 根（2025-07-22 至 2026-07-21）。
- 公司研究真实联调：`600519`、`000001`、`300502` 均成功取得 12 个季度财务、近三年公告和估值结果；行业映射分别覆盖消费、金融和通信制造。
- X 未提供用户 Cookie：设置页显示 `not_configured`，同步安全暂停。
- LLM 未提供 API Key：交易计划页面显示“未配置AI分析；规则引擎不受影响”。

## 已知限制

- 回测未完整模拟 A 股 T+1、涨跌停、停牌和成交量约束，页面与结果均显示提示。
- 当前没有用户登录和权限系统，只应监听本机或部署在可信内网。
- AKShare 和 X 均为外部接口，可能发生结构变化、限流或服务不可用。
- MySQL Docker 配置未在当前电脑实跑。
- 交易计划尚未接入可靠 60 分钟 K 线、换手率、宽基市场自动状态和行业指数自动强弱；当前明确显示缺失或由用户确认。
- 产业链节点相关性仍使用持仓板块/公司行业的简化匹配；尚未完成可导航产业链图。
- AI 已有公司、财报、公告、估值和研究证据包，但尚未直接接入上交所/深交所独立公告接口、官方产业高频数据和完整同行估值时间序列。
