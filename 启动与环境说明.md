# 股票纪律助手：启动与环境说明

本文档对应项目目录：`D:\pythonproject\new`。

## 1. 当前电脑环境检查结果

检查日期：2026-07-22。

| 项目 | 当前状态 | 是否影响本地启动 |
|---|---|---|
| Python | 已安装，3.11.9 | 不影响 |
| pip | 已安装，25.1.1 | 不影响 |
| PowerShell | 已安装，5.1 | 不影响 |
| 项目 Python 依赖 | 已安装 | 不影响 |
| Git | 已安装，2.42.0 | 不是运行必需 |
| Docker / Docker Compose | **未安装或命令不可用** | 不影响 SQLite 本地启动，但无法启动 MySQL 容器版 |
| MySQL | 未单独检查/无需本地安装 | SQLite 模式不需要；Docker 模式由 Compose 提供 |

结论：**当前电脑已经可以直接用 SQLite 启动项目。现在唯一明确缺少的是 Docker，因此暂时不能运行 Docker + MySQL 版本。**

## 2. 最简单的启动方式：Windows + SQLite

打开 PowerShell，执行：

```powershell
Set-Location -LiteralPath "D:\pythonproject\new"
.\start.ps1
```

首次运行时，脚本会自动完成：

1. 创建 `.venv` Python 虚拟环境；
2. 安装项目及测试依赖；
3. 执行 Alembic 数据库迁移；
4. 在 `127.0.0.1:8000` 启动服务。

启动完成后访问：

- 系统首页：<http://127.0.0.1:8000/>
- API 文档：<http://127.0.0.1:8000/docs>
- 健康检查：<http://127.0.0.1:8000/api/v1/health>

健康检查正常时应返回类似内容：

```json
{
  "status": "ok",
  "database": "ok",
  "version": "0.1.0"
}
```

以后依赖没有变化时，可以跳过依赖安装：

```powershell
.\start.ps1 -SkipInstall
```

停止服务：回到运行服务的 PowerShell 窗口，按 `Ctrl+C`。

## 3. 如果 PowerShell 禁止运行脚本

如果出现“running scripts is disabled”或“无法加载文件，因为在此系统上禁止运行脚本”，无需永久修改系统策略，可以仅为当前 PowerShell 进程临时放行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\start.ps1
```

关闭该 PowerShell 窗口后，临时设置自动失效。

也可以完全不运行脚本，手工启动：

```powershell
Set-Location -LiteralPath "D:\pythonproject\new"
python -m pip install -e ".[dev]"
python -m alembic upgrade head
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

## 4. 环境配置文件

本地 SQLite 启动不要求创建 `.env`，项目会使用安全默认值。如果需要自定义配置，先复制示例：

```powershell
Copy-Item -LiteralPath ".env.example" -Destination ".env"
```

常用配置：

```dotenv
APP_NAME=股票纪律助手
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
LOG_LEVEL=INFO
DATABASE_URL=sqlite:///./stock_assistant.db
```

注意：

- `.env` 已被 `.gitignore` 排除，不要将真实密码、Cookie 或 API Key 提交到 Git。
- 默认 SQLite 数据文件为项目目录下的 `stock_assistant.db`。
- 本地环境只监听 `127.0.0.1`，不会默认对局域网或公网开放。

## 5. 当前不需要、后续功能才需要的凭据

这些内容目前都不是账户/持仓和 SQLite 启动的前置条件：

| 配置 | 当前状态 | 用途 |
|---|---|---|
| `LLM_BASE_URL` | 可留空 | 交易计划的 AI 证据归纳和已有资讯摘要 |
| `LLM_API_KEY` | 可留空 | AI 分析鉴权；只从环境变量读取，不写入前端/数据库 |
| `LLM_MODEL` | 可留空 | 指定 OpenAI-compatible 模型 |
| `X_COOKIE` | 可留空 | 后续低频采集用户有权访问的 X 公开内容 |
| AKShare | 已安装并接入 | A 股公开行情、历史数据、指数、板块与公告 |
| twscrape | 已安装并接入 | X 信息监控；实际使用需要自有合法 Cookie |
| backtesting.py | 已安装并接入 | 交易规则历史验证、资金/回撤曲线和逐笔模拟交易 |
| pandas-ta-classic | 已安装并接入 | 均线、MACD、RSI、ATR、ADX、布林带、OBV 等指标 |
| vectorbt | 已安装并接入 | 分别验证技术信号的历史样本、胜率、收益和回撤 |
| httpx / pypdf | 已安装并接入 | 公告原文定位与最新年报 PDF 证据提取 |

没有上述凭据时，系统不得伪造行情或 AI 结果，也不会阻止基础功能启动。

X Cookie 可使用 twscrape 支持的 `name=value; name=value` 格式，至少需要你自己登录会话中的 `auth_token` 和 `ct0`。不要把 Cookie 发给他人或提交到 Git；系统只允许采集你有权访问的公开内容。

OpenAI-compatible 示例：

```dotenv
LLM_BASE_URL=https://你的服务地址/v1
LLM_API_KEY=你的密钥
LLM_MODEL=你的模型名称
LLM_ENABLED=true
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=1
LLM_MAX_INPUT_CHARS=40000
LLM_DAILY_LIMIT=20
LLM_CACHE_HOURS=24
```

服务必须兼容 `POST /chat/completions` 和 JSON response format。未配置模型时，交易计划的行情、技术、硬止损、仓位、状态和保存功能仍可使用；AI 区域会明确显示未配置。

如使用 Gemini，先在 Google AI Studio 创建 API Key，然后运行：

```powershell
.\configure-gemini.ps1
```

脚本默认配置 `gemini-3.6-flash`，先验证鉴权再写入 `.env`，不会把密钥写入数据库或前端。配置后需要重启正在运行的 Uvicorn 进程。

## 6. Docker + MySQL 启动

### 当前缺少的环境

当前电脑找不到 `docker` 命令。若要使用容器版，需要先安装并启动 Docker Desktop，确保下面两条命令都能成功：

```powershell
docker --version
docker compose version
```

安装完成后，在项目目录创建 `.env` 并至少修改数据库密码：

```dotenv
MYSQL_PASSWORD=请替换为强密码
MYSQL_ROOT_PASSWORD=请替换为另一个强密码
```

然后运行：

```powershell
Set-Location -LiteralPath "D:\pythonproject\new"
docker compose up --build
```

容器启动后仍访问 <http://127.0.0.1:8000/>。

停止容器：

```powershell
docker compose down
```

`docker compose down` 不会删除 MySQL 数据卷。除非明确确认不再需要数据，否则不要执行带 `-v` 的删除命令。

## 7. 启动前后自检命令

检查 Python 和主要依赖：

```powershell
python --version
python -c "import fastapi, sqlalchemy, alembic, uvicorn; print('依赖正常')"
```

检查数据库迁移：

```powershell
python -m alembic current
python -m alembic check
```

运行自动测试：

```powershell
python -m pytest
python -m ruff check app tests
```

当前项目基线为：40 项测试通过、Ruff 检查通过。

## 8. 常见启动问题

### 找不到 Python

确认 `python --version` 可执行。若不可执行，安装 Python 3.11 或 3.12，并将 Python 加入 `PATH`。

### 缺少 Python 模块

```powershell
python -m pip install -e ".[dev]"
```

### 端口 8000 被占用

改用其他端口：

```powershell
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

然后访问 <http://127.0.0.1:8001/>。

### SQLite 数据库异常，需要重新建空库

先停止应用并备份 `stock_assistant.db`，不要直接删除真实交易数据。需要全新体验时，可以在 `.env` 中改用另一个文件：

```dotenv
DATABASE_URL=sqlite:///./stock_assistant_new.db
```

然后重新运行：

```powershell
python -m alembic upgrade head
```

### Docker 命令不可用

这是当前电脑已经确认存在的缺项。安装 Docker Desktop、启动其后台服务，并重新打开 PowerShell 后再检查 `docker compose version`。

### AKShare 同步返回 503 / ProxyError

本机 Clash 代理下，东方财富接口可能主动断开。项目已实现自动降级：

- 实时价：东方财富 → 新浪；
- 前复权日线：东方财富 → 腾讯 → 新浪；
- 若实时价都暂时不可用但历史线成功，同步仍会使用最新一根历史收盘价，并明确标记来源。

2026-07-22 已用 `300502` 实测：新浪实时价成功，腾讯前复权日线成功返回 242 根。更新代码后先在原服务窗口按 `Ctrl+C`，再运行：

```powershell
.\start.ps1 -SkipInstall
```

然后重新点击市场同步。成功响应中的 `fallback_used` 会是 `true`，`history_source` 会显示实际备用源。

## 9. 当前可用范围

当前版本已经可以：

- 创建、修改、查询和删除账户；
- 创建、修改、查询和删除持仓；
- 使用总资产（包含现金）计算持仓比例；
- 计算持仓市值、浮盈亏和收益率；
- 使用 SQLite 保存数据；
- 查看 API 文档和健康状态。
- 检查仓位、止损、计划外、情绪化和过度交易等纪律风险；
- 导入交易 CSV，查看日/周复盘；
- 从 AKShare 同步真实行情、历史日线、指数、板块和公告；
- 使用自有 Cookie 低频同步 X 公开信息并去重；
- 使用 OpenAI-compatible 服务生成结构化翻译与研究摘要；
- 使用手续费和滑点运行多种交易规则历史验证，与“买入并持有”比较；显示关键指标、资金/回撤曲线、逐笔交易、普通中文结论和样本不足警告；
- 分析复权 K 线、指标状态、MACD 背离、量价信号、支撑压力与指标矛盾；支撑压力包含 ATR 限宽、连续触碰合并、时间衰减、震荡/争夺区及突破/跌破确认和失效条件；
- 使用 vectorbt 回答各类信号过去的样本量、胜率、平均收益和最大回撤；
- 每个交易日为持仓生成技术状态变化记录；
- 在“公司研究中心”同步最近 12 个季度财报、近三年公告、历史估值和同行数据；
- 查看经营/财务趋势、红黄灰风险雷达、公告原文、估值情景和产业证据时间线；
- 可选解析最新年报 PDF；该功能无需 LLM，但下载和解析时间会比普通同步长；
- 在“候选池与交易计划”填写事前计划，按风险预算、仓位上限、现金和 A 股整手计算数量；
- 在“今日工作台”查看持仓风险、接近计划区的候选、纪律事件和失败/过期数据；
- 在“持仓管理”检查硬止损、规则版本、是否允许加仓和下一步动作并保存状态快照；
- 可选启用日/周复盘及 X 定时任务。

不配置 X Cookie 或 LLM API Key 时，相应模块会明确显示未配置，账户、持仓、纪律、复盘、AKShare 和回测等其他功能仍可使用。
